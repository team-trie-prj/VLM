# 핵심 의존성 — GPU 없이 Windows에서 바로 설치 가능 (mock 백엔드 실행에 충분)
pydantic>=2.5
pillow>=10.0
pyyaml>=6.0
python-dotenv>=1.0

# --- API 백엔드 (키가 생기면 주석 해제하여 설치) ---------------------------
# google-genai  # Gemini 비전 (무료 티어). https://aistudio.google.com 무료 키
# anthropic     # Claude 비전 (messages.parse 구조화 출력). 최신 버전 권장
# openai        # GPT-4o 비전

# --- 데모 UI (선택) -------------------------------------------------------
# gradio      # python -m vlm demo (업로드→질의→탐지→오버레이 시연)

# --- 실제 데이터 확보 (선택) ----------------------------------------------
# roboflow    # Roboflow Universe 도로손상 데이터셋 다운로드 (scripts/fetch_roboflow.py)

# --- 로컬 Qwen2.5-VL 백엔드는 requirements-local.txt 참고 ----------------
